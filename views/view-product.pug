extends layouts/main-layout.pug

block content
    .container
        .product-container
            if prod
                h4= prod.name
            else
                h2 Error: No product to display